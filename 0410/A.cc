#include <cstdio>
#include <cstring>
#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
	int T_T; scanf("%d", &T_T);
	while (T_T--)
	{
		int n; scanf("%d", &n);
		puts(n ? "You are the future of Tencent!" : "Good luck and Enjoy TPC!");
	}
	return 0;
}
