#include <cstdio>
#include <cstring>
#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
	int _; scanf("%d", &_);
	while (_--)
	{
		int n; scanf("%d", &n);
		if (n)
			puts("You are the future of Tencent!");
		else
			puts("Good luck and Enjoy TPC!");
	}
	return 0;
}
